import { RosterPositionPipe } from './roster-position.pipe';

describe('RosterPositionPipe', () => {
  it('create an instance', () => {
    const pipe = new RosterPositionPipe();
    expect(pipe).toBeTruthy();
  });
});
