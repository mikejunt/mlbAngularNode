import { TeamDetailsPipe } from './team-details.pipe';

describe('TeamDetailsPipe', () => {
  it('create an instance', () => {
    const pipe = new TeamDetailsPipe();
    expect(pipe).toBeTruthy();
  });
});
