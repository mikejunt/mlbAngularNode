import { LeadersDescPipe } from './leaders-desc.pipe';

describe('LeadersDescPipe', () => {
  it('create an instance', () => {
    const pipe = new LeadersDescPipe();
    expect(pipe).toBeTruthy();
  });
});
