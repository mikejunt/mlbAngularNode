import { SortRosterPipe } from './sort-roster.pipe';

describe('SortRosterPipe', () => {
  it('create an instance', () => {
    const pipe = new SortRosterPipe();
    expect(pipe).toBeTruthy();
  });
});
