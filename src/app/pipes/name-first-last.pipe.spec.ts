import { NameFirstLastPipe } from './name-first-last.pipe';

describe('NameFirstLastPipe', () => {
  it('create an instance', () => {
    const pipe = new NameFirstLastPipe();
    expect(pipe).toBeTruthy();
  });
});
